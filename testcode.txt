Source Range: "A2:Z2" (or whatever your column width is).

Destination Range: "A" & (dtReportData.Rows.Count + 2).ToString & ":Z" & (dtReportData.Rows.Count + 2 + in_ONITReportData.Rows.Count).ToString

Action: Select Formats (this mimics the "Format Painter" in Excel).
